<span class="threshold">Threshold
<?php
    print $view->getThresholdButton('++', 0.1, $threshold);
    print $view->getThresholdButton('+', 0.01, $threshold);
    print $view->getThresholdButton('-', -0.01, $threshold);
    print $view->getThresholdButton('--', -0.1, $threshold);
?>
</span>
